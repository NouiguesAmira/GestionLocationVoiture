{% extends 'admin/base.html.twig' %}

{% block body %}
    <h1>{{ 'Create new marque'|trans }}</h1>

    {% include 'admin/marque/_form.html.twig' with {'form': form} only %}

    <a href="{{ path('admin.marque.list') }}" class="btn btn-default">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
        {{ 'back to list'|trans }}
    </a>
{% endblock %}
