{% extends 'admin/base.html.twig' %}

{% form_theme form _self %}

{% block _voiture_logo_widget %}
    {% if form.vars.data %}
        <img class="media-object" style="width:100px; height:100px;" src="{{ asset(jobs_web_directory ~ '/' ~ form.vars.data.filename) }}">
    {% endif %}

    {{ form_widget(form) }}
{% endblock %}

{% block body %}
    <h1>{{ 'Edit voiture'|trans }}</h1>

    {% include 'admin/voiture/_form.html.twig' %}

    <a href="{{ path('admin.voiture.list') }}" class="btn btn-default">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
        {{ 'back to list'|trans }}
    </a>
{% endblock %}
